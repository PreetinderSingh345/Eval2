const {Company} = require('../../database/models');

const saveCompanies = async (urlLink) => {
    const output= await "Please return this";

    return urlLink;
};

module.exports = {saveCompanies};